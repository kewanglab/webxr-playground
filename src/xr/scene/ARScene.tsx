export function ARScene() {
  // AR uses passthrough — no virtual floor or environment needed.
  // AR-specific helpers (placement reticles, anchor indicators)
  // will be added here in Phase 2.
  return null
}
